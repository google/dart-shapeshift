name: shapeshift
description: Understand how the shape of your Dart package is shifting.
environment:
  sdk: '>=1.9.0 <2.0.0'
dependencies:
  args: any
  diff_match_patch: ^0.2.1
  doc_coverage: ^0.1.5
  json_diff: ^0.1.2
  markdown: ^0.7.1+2
  path: ^1.3.3
  pub_semver: ^1.2.1
  quiver: ^0.21.3+1
dev_dependencies:
  test: ^0.12.0
